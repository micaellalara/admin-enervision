<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EnerVision Admin</title>
    <link rel="shortcut icon" href="/images/enervision.png" />
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Montserrat';
            background: linear-gradient(135deg, #C3E0D2, #F0E8D9);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .split-container {
            display: flex;
            flex-direction: row;
            background-color: #F5F5F2;
            border-radius: 30px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
            overflow: hidden;
            max-width: 1000px;
            width: 90%;
        }

        .left-panel {
            background: linear-gradient(160deg, #A7C5BD, #E5E8B6);
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px;
            position: relative;
        }

        .left-panel img {
            max-width: 80%;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .right-panel {
            flex: 1.5;
            background-color: #F0E8D9;
            padding: 60px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .right-panel h1 {
            font-size: 38px;
            color: #3D5A40;
            margin-bottom: 20px;
            font-weight: bold;
        }

        .right-panel p {
            font-size: 18px;
            color: #6A706E;
            margin-bottom: 40px;
            text-align: center;
            max-width: 85%;
        }

        .right-panel::before,
        .right-panel::after {
            content: '';
            position: absolute;
            border-radius: 50%;
            opacity: 0.5;
            background-color: #E5E8B6;
        }

        .right-panel::before {
            width: 300px;
            height: 300px;
            top: -50px;
            left: -100px;
            background: radial-gradient(circle at 50% 50%, #C3E0D2, #A7C5BD);
        }

        .right-panel::after {
            width: 200px;
            height: 200px;
            bottom: -70px;
            right: -50px;
            background: radial-gradient(circle at 50% 50%, #A7C5BD, #E5E8B6);
        }

        .btn-custom {
            background-color: #3D5A40;
            color: white;
            padding: 12px 40px;
            font-size: 18px;
            border-radius: 30px;
            margin: 10px;
            border: none;
            transition: all 0.3s ease;
        }

        .btn-custom:hover {
            background-color: #247968; 
            color: #F4F1DE;
            box-shadow: 0 8px 20px rgba(129, 178, 154, 0.6);
        }

        @media (max-width: 768px) {
            .split-container {
                flex-direction: column;
                max-width: 100%;
                height: auto;
            }

            .left-panel {
                padding: 20px;
            }

            .right-panel {
                padding: 30px;
            }
        }
    </style>
</head>

<body>
    <div class="split-container">
        <div class="left-panel">
            <img src="/images/enervision.png" alt="EnerVision Logo">
        </div>

        <div class="right-panel">
            <h1>Welcome to EnerVision Dashboard!</h1>
            <p>Begin your journey with us to a greener, smarter energy future.</p>
            <div>
                <a href="/login" class="btn btn-custom" style="width: 250px;">Login</a>
                <!-- <a href="/register" class="btn btn-custom">Sign Up</a> -->
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
